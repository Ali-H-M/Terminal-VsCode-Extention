# Open the Settings Panel

The settings panel is your hub for creating and managing terminal profiles.

Click **Open Settings** above, or run `Terminal Launcher: Open Settings` from the Command Palette (`Ctrl+Shift+P`).
